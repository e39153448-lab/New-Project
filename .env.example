DATABASE_URL="postgresql://postgres:postgres@localhost:5432/rocketclub?schema=public"
AUTH_SECRET="replace-with-a-long-random-string-in-production"
CALENDLY_URL="https://calendly.com/rocketclub/call"

# Shared secret required to hit /api/admin/seed (GET or POST) to (re)seed demo data.
# Leave unset to disable the endpoint entirely.
SEED_SECRET="replace-with-a-long-random-string"
